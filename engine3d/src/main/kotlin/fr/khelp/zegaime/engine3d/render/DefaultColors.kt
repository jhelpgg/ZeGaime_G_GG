package fr.khelp.zegaime.engine3d.render

import java.awt.Color

/**
 * Default black color.
 */
val BLACK = Color4f(0f)

/**
 * Default blue color.
 */
val BLUE = Color4f(0f, 0f, 1f)

/**
 * Default dark gray color.
 */
val DARK_GRAY = Color4f(0.25f)

/**
 * Default dark red color.
 */
val DARK_RED = Color4f(0.5f, 0f, 0f)

/**
 * Default dark green color.
 */
val DARK_GREEN = Color4f(0f, 0.5f, 0f)

/**
 * Default dark blue color.
 */
val DARK_BLUE = Color4f(0f, 0f, 0.5f)

/**
 * Default wire frame color : black semi-transparent.
 */
val DEFAULT_WIRE_FRAME_COLOR = Color4f(0f, alpha = 0.5f)

/**
 * Default gray color.
 */
val GRAY = Color4f(0.5f)

/**
 * Default green color.
 */
val GREEN = Color4f(0f, 1f, 0f)

/**
 * Default light blue color.
 */
val LIGHT_BLUE = Color4f(0.5f, 0.5f, 1f)

/**
 * Default light gray color.
 */
val LIGHT_GRAY = Color4f(0.75f)

/**
 * Default light green color.
 */
val LIGHT_GREEN = Color4f(0.5f, 1f, 0.5f)

/**
 * Default light red color.
 */
val LIGHT_RED = Color4f(1f, 0.5f, 0.5f)

/**
 * Default red color.
 */
val RED = Color4f(1f, 0f, 0f)

/**
 * Default white color.
 */
val WHITE = Color4f(1f)

/**
 * Default yellow color.
 */
val YELLOW = Color4f(1f, 1f, 0f)

/**
 * Default orange color.
 */
val ORANGE = Color4f(Color.ORANGE.rgb)

/**
 * Default arm color.
 */
val COLOR_ARM = Color4f(0xFF828105.toInt())

/**
 * Default leg color.
 */
val COLOR_LEG = Color4f(0xFF020260.toInt())